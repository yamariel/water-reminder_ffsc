import 'package:flutter/material.dart';

class HistoryPagePage extends StatelessWidget {
  const HistoryPagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Page des historique'));
  }
}
